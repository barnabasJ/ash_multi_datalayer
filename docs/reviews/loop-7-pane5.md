1. `docs/tasks/20260707-second-review-fixes/00-index.md:42-44` still says the holistic review added an "all five tenant paths agree" assertion to B3. That contradicts the binding tenant-unit instructions in the same index (`00-index.md:133-147`, four bucketing paths plus separate target-call tenant values) and B3 itself (`b3-tenant-from-filter-dead-code.md:3-7`, `:99-106`, four bucketing call sites and target calls excluded). Current source matches the four-path model: target pushes pass the real tenant to the delegated layer in `lib/ash_multi_datalayer/orchestrator/local_outbox/flush.ex:163-178`, and the stale-check read passes `entry.tenant` at `flush.ex:196-197`, while bucketing/filter paths are separate (`api.ex:559-566`). Leaving the "all five" wording makes the tracker self-contradictory and can be satisfied incorrectly by forcing target calls to equal the partition key, which the index explicitly says would be wrong.
