1. `final-gates.md:23-26` makes the offline demo gate impossible as written. It says to exercise the example flow via every verb, including `discard` and `rebase`, but `../ash_remote/example/README.md:100-103` documents only the three-way UI, and `../ash_remote/example/todo_client/lib/todo_client/offline_live.ex:268-277` renders buttons only for `force`, `discard_local`, and `retry`. `discard` is only accepted by the hidden event handler at `offline_live.ex:161-166`, and `rebase` is not handled by the UI at all; it exists only as `LocalOutbox.rebase/2` with a required changeset (`lib/ash_multi_datalayer/orchestrator/local_outbox/api.ex:235-258`). An implementer can complete the documented demo while `discard`/`rebase` remain unexercised, or be blocked trying to satisfy a UI flow that does not exist.

2. `l1-composite-pk-aggregate-paths.md:30-33` asks for a composite-PK partial/remainder read that exercises `pk_merge/3`, but that production path is currently unreachable for composite-PK resources. `pk_merge/3` still has the `[pk] = Ash.Resource.Info.primary_key(resource)` match at `lib/ash_multi_datalayer/orchestrator/proven_coverage.ex:597-600`, but its only caller is behind `remainder_applicable?/2`, which requires `match?([_], Ash.Resource.Info.primary_key(resource))` at `proven_coverage.ex:545-551`. The requested black-box repro cannot fail on current code for the stated reason unless it bypasses private code or first changes the single-PK guard, so the acceptance criterion is impossible/misleading.

3. `00-index.md:145` points implementers to `flush.ex:167-178` for delegated-layer “push/stale-check” tenant threading, but that range only covers the `Target.destroy/3` and `Target.upsert/3` push calls. The stale-check target read is a separate `Target.read_pk(..., tenant: entry.tenant)` at `lib/ash_multi_datalayer/orchestrator/local_outbox/flush.ex:196-198`. This is a misleading line reference because an implementer following it could update only push calls and miss the load-bearing stale-check tenant call.
