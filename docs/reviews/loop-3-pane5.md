## Findings

1. **MEDIUM - B3 still gives contradictory cardinality for the tenant assertion.** `docs/tasks/20260707-second-review-fixes/b3-tenant-from-filter-dead-code.md:3-6` says B3 closes only after a "five-call-site cross-cutting assertion" passes, but the actual acceptance criterion in the same file says the assertion covers the **four bucketing call sites** and explicitly excludes target calls (`b3-tenant-from-filter-dead-code.md:97-104`). The binding index agrees that the partition-key concept covers four paths - ledger/coverage, write invalidation, outbox chain filters, and notifications - while target calls carry the real tenant value instead (`00-index.md:133-155`). As written, an implementer can either chase a nonexistent fifth bucketing path or incorrectly include target calls in the equality assertion, contradicting concept 3. Change the status/summary wording to four bucketing call sites and keep target calls out.

2. **MEDIUM - Unscoped-sentinel ownership is split between B3 and H5.** The index says B3 owns the canonical partition-key function and H5 "additionally owns the unscoped sentinel" (`00-index.md:151-154`). B3's task then says B3 must pick the sentinel and make every path use it (`b3-tenant-from-filter-dead-code.md:81-83`, `b3-tenant-from-filter-dead-code.md:90-93`), while H5 says both that B3 chooses it (`h5-localoutbox-nil-tenant-model.md:42-45`) and that H5 owns it (`h5-localoutbox-nil-tenant-model.md:61-63`). This creates a sequencing trap: B3 can be considered incomplete until it implements an H5-owned concept, while H5 can be considered dependent on a B3 decision it supposedly owns. Pick one owner and make the other task consume it.

3. **LOW - L12's ETS insert line references are stale.** `docs/tasks/20260707-second-review-fixes/l12-mdl-misc-lows.md:15-19` says the `:ets.insert` call is at `coverage.ex:57` and `def insert(resource, tenant, entry)` is at `:55`. In the current source, `def insert/3` is at `lib/ash_multi_datalayer/coverage.ex:56` and the `:ets.insert` call is at `coverage.ex:58`. Update the line references so the task points to the actual load-bearing lines.
